# Narrator startup/focus guard at b179fbf2

[Run 33842863109](https://github.com/pawaovo/shadcn_flutter/actions/runs/33842863109)
independently verifies the requested checkout
`b179fbf2679074ecddfd6d9fa00b318e20d641d7` and its four recorded source hashes.
The workflow definition was dispatched from the preceding 04989268 head while
a transient push error was being resolved; checkout verification passed before
any test executed. Both identities are kept in the [JSON record](narrator-b179fbf2.json).

Actual Windows PowerShell 5.1 compilation, **30 pure C# guard/diagnostic checks**
and JSON round-trip pass in **1.984 seconds**. The real probe exits unaccepted
after **13.307 seconds**. Ordinary guarded Tab/Space succeed, but no candidate
passes the then-used top-level `ControlType.Window` filter within eight seconds.
Minimize requests are zero and no Narrator command follows that preparation
failure. The report does not establish whether an owned HWND was exposed as a
pane instead; the fixture's actual control-type records were panes.

The all-state render endpoint count is again **0**. Preparation, read words,
reader invocation and synthesized audio remain separate unaccepted boundaries.
The subsequent discovery adjustment uses actual owned HWNDs and WindowPattern
support, preserving PID/lifetime checks, single-action behavior, deadlines and
all AT predicates. This original failed attempt remains intact.

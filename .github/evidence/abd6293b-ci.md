# Exact-source CI checkpoint: abd6293b

Both attempt-1 workflows finished naturally at `abd6293b066f2df56108cebfe3689fcc5b9a0a13`.

| Workflow | Result | Evidence |
| --- | --- | --- |
| Main | **11/12** | [33843835738](https://github.com/pawaovo/shadcn_flutter/actions/runs/33843835738) |
| Input / AT capability | **4/9** | [33843835663](https://github.com/pawaovo/shadcn_flutter/actions/runs/33843835663) |

Android passes its complete journey with the stronger one-tap Chat host-message and responding-state checks. The original full shared Edge journey also passes. All platform release builds, source quality checks, package validation and the other complete desktop/browser journeys pass. The Apple job fails later in iOS VM-service discovery, after successful build, install and launch; the native iOS driver is **not started**.

The first 15-second unified-log query times out without output. The subsequent one-second `/bin/ps` membership query also times out, leaving cleanup unverified; the original failure is retained. A kernel `signal 0` absence proof is being added as a separate tested improvement: only `ProcessLookupError` can prove an absent group without spawning ps. Present/permission-denied groups retain the original inspection and failure rules, all timeouts and the original 120-second discovery budget.

All four complete trusted W3C input suites and all three native framework suites pass. The Linux native fixture capability now passes its original navigation, utterance and actual PCM requirements after the raw PTY transport repair: 53,746 frames, RMS 3869.65. This fixture is separate from real Catalog acceptance and human listening. Browser framework composing assertions and Narrator remain unaccepted; Edge framework still fails startup before application assertions. [The new original Edge failure resource analysis](../../docs/beautiful-ui/diagnostics/edge-startup/2026-09-04-abd6293b-startup.md) distinguishes startup I/O waiting from the later 300-second navigation wait.

[The JSON companion](abd6293b-ci.json) records the 21 job identities, exact source, suite outcomes, iOS query failure and raw artifact hashes. Neither original workflow was retried or converted into a passing record. The separate updated [Linux Catalog run](../../docs/beautiful-ui/diagnostics/linux-sdk-runtime-build/evidence/catalog-79fbcdd1-orca.md) uses a newer Thinking component and explicitly rebuilt native dependencies.

# Exact-source CI checkpoint: f15f27eb

Source: `f15f27eb671086444f137475f1cf4e9de3eb6328`. Both runs completed naturally on attempt 1; original failures were retained.

| Workflow | Result | Evidence |
| --- | --- | --- |
| Main | **10/12** | [33839986027](https://github.com/pawaovo/shadcn_flutter/actions/runs/33839986027) |
| Input / AT capability | **3/9** | [33839985973](https://github.com/pawaovo/shadcn_flutter/actions/runs/33839985973) |

Main passes source formatting/analysis/tests, all platform builds, package validation and the Chrome/Linux, Firefox, Windows, macOS and actual iOS journeys. The iOS required driver passes with exit 0 in **56.534 seconds**, with the report recording `passed=true`. Android boots, builds and installs successfully, then fails in P2 Chat because `Stop response` is absent after Send. The preceding text finder also matches the editor draft, so that assertion alone cannot prove host acceptance. The actual send failure cause remains unconfirmed; a separate diagnostic preserves one original tap and adds public host-state checks. Edge again fails its initial browser navigation/window-position request before Catalog's shared journey runs.

All **four complete trusted W3C input suites pass**, as does Safari's separate original journey. All three native framework input suites pass. Chrome/Firefox fail the injected composing-range check before the corresponding Enter; Safari fails the injected range before its resize operation; Edge's framework failure is browser startup. These results neither accept nor disprove real OS IME behavior.

The Linux capability fixture reaches beta and its real Where Am I handler, but a new utterance is not visible in the buffered debug file within the original deadline. [The log-transport diagnosis](../../docs/beautiful-ui/quality_evidence/2026-09-04-orca-capability-log-transport.md) keeps this run unaccepted and supplies a separate red/green transport repair for the next actual execution. The current Windows fixture also remains unaccepted; its later [dedicated endpoint/command diagnosis](narrator-70e3d149.md) has its own source and run.

A separate [valid native macOS P3 capture](../../docs/beautiful-ui/quality_evidence/performance/2026-09-04-f15f27eb-display-awake.md) at this exact source passes **7/7 unchanged engineering budgets**, with 5,163 frames and 525 RSS samples. This does not override any CI failure or grant all-platform release acceptance. All 27 registry items remain in progress and all six platforms remain Partial.

[The JSON companion](f15f27eb-ci.json) preserves all 21 job identities, source/run metadata, independent suite outcomes, iOS stage results and hashes of the downloaded raw artifacts. Raw files remain under `/tmp/beautiful-ci-f15f27eb`; the Android job log is saved separately at the path recorded in JSON. Authenticated VM-service addresses are not copied into this summary.

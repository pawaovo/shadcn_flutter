# Completed main CI for `75594991`

The [single main workflow run 33863483982](https://github.com/pawaovo/shadcn_flutter/actions/runs/33863483982)
completed naturally on attempt 1 at
`7559499112dcc5c5d9b0370ba1d6b0eb34743f45`: **11/12 jobs passed**. The sole
failure is the original Android P3 Prompt assertion, after its actual native
candidate commit and original Chat Send succeeded. No workflow was retried.

| Gate | Actual result |
|---|---|
| Format and strict analysis | 170 Dart files, zero changes; three packages clean |
| Library tests | 537 behavior + 109 semantics + 12 strict Linux goldens = **658 passed** |
| Catalog tests | **147 passed** |
| Upstream core tests | **571 passed** |
| Platform builds | Android, Linux, Windows, iOS, macOS, Web JavaScript and WebAssembly passed |
| Original complete journeys | Chrome, Edge, Firefox, Linux, Windows, macOS and iOS passed |
| Android original journey | Failed at P3 `/rest` Enter assertion; complete failure Response received |
| Publication validation | 3 MB dry-run archive, zero warnings; isolated consumer passed |

The [JSON companion](75594991-ci.json) binds all 12 job identities, source,
attempt, nine uploaded artifacts and original-file hashes. Each artifact was
downloaded once; **174 extracted files** were independently hashed. ZIP
digests are GitHub-reported and are distinguished from those local file hashes.
Raw evidence remains at `/tmp/beautiful-75594991-main-33863483982`.

The [Android evidence](android-candidate-75594991.md) separately records actual
LatinIME candidate identity, one native DOWN/UP, unchanged text with composition
cleared, the final fresh Send guard and one original Chat Send. The corrected
Send-to-Stop observer completed without error. The journey then continued to
`catalog_journey_test.dart:304`, where the original Enter expected `/restock `
but text remained `/rest`. The complete original Response reports failure;
the contemporaneous Prompt focus, menu and composing state were not captured,
so this record does not assert their cause. Driver and supervisor cleanup both
have empty error lists, verified owned cleanup and confirmed native drain.
This result does not rewrite the earlier
[5a90 observer failure](android-candidate-5a90dff9.md).

Chrome 152.0.7977.64, Firefox 154.0 and Edge 151.0.4129.101 each completed the
original main journey. Their reports preserve the two trusted browser Copy
actions and the Flutter-injected scope of the remaining actions. Edge's
existing once-only executable preread condition also verified the actual
session executable and the absence of live processes among its observed
identities. This single run does not establish a general startup success rate
or absence of every unobserved descendant.

The actual [Apple job](https://github.com/pawaovo/shadcn_flutter/actions/runs/33863483982/job/100993028181)
passed both complete native journeys. iOS installation took **22.430 s**,
launch **7.723 s**, VM discovery **35.903 s** including launch, and the actual
native Flutter driver **42.662 s**, exiting zero. Its original bounded
discovery loop made two history queries: the first timed out after its
15-second limit with exit 124, zero bytes and verified process-group cleanup;
the second exited zero after **10.195 s**, read 227 bytes and also fully
drained. Both report stdout EOF and `host_group_clean=true`. The application
was terminated successfully afterward. This was an internal discovery poll,
not a workflow retry or a changed timeout.

The isolated consumer used public `shadcn_flutter 0.0.54`, verified **209
unmodified hosted runtime files**, **12 theme observations** and **13 required
license labels**. `beautiful_ai_ui` was a temporary publication-surface path
dependency; no package was published.

**No input/AT workflow was dispatched at this source.** The historical
[5edbcab7 input/AT run](5edbcab7-ci.md) remains **4/9 jobs passed** at its own
source, with its four successful trusted browser suites and all original
framework/Narrator failures preserved. Those records are not presented as new
75594991 runs. These main CI results also do not establish full-platform,
human IME, human assistive-technology or native performance acceptance.

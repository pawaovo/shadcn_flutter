# Edge full journey pair at 153412b3

Source: `153412b3014192b2d6c4c15e377161a5a862a968`.
[Manual run 33848023805](https://github.com/pawaovo/shadcn_flutter/actions/runs/33848023805)
finished with **failure**: baseline timed out before Catalog navigation, while
preread completed the original full Catalog journey. The comparison job passed
with `provenance_matched` and no blockers. This is one pair on different fresh
runners; it does not establish a cause, fix or failure/reproduction rate.

This document derives from the downloaded originals under
`/tmp/beautiful-edge-pair-153412b3-33848023805/`. No new run or retry was performed
to prepare it. The accompanying [JSON record](2026-09-04-153412b3-pair.json)
preserves run/job metadata, exact original paths, archive metadata, file hashes,
command boundaries and interpretation limits. The
[earlier startup analysis](2026-09-04-abd6293b-startup.md) explains the question
that motivated this pair; its separate historical results remain unchanged.

## Original outcomes

| Job | Result | Actual execution boundary | Completed UTC |
| --- | --- | --- | --- |
| [Full Edge journey - baseline](https://github.com/pawaovo/shadcn_flutter/actions/runs/33848023805/job/100944254622) | failure / suite exit 1 | First window-position command timed out; no Catalog navigation | 2026-09-04 07:24:26 |
| [Full Edge journey - preread](https://github.com/pawaovo/shadcn_flutter/actions/runs/33848023805/job/100944254349) | success / suite exit 0 | Original full Catalog journey passed | 2026-09-04 07:20:51 |
| [Check actual pair provenance and retain both outcomes](https://github.com/pawaovo/shadcn_flutter/actions/runs/33848023805/job/100945952958) | success | Compared recorded provenance and retained both outcomes | 2026-09-04 07:24:39 |

Run metadata reached its final update at `2026-09-04T07:24:40Z`; attempt is 1.
All three jobs printed the same full checkout HEAD as the run's `headSha`:
`153412b3014192b2d6c4c15e377161a5a862a968`. The retained top-level `run.log`
contains those HEAD outputs at lines 131, 526 and 908. Both journey jobs also
ran the recorded HEAD equality check (lines 142 and 537). Each condition invokes
the same `flutter drive` target and trusted journey driver, requests `1440x900`,
and sets `CATALOG_TRUSTED_BROWSER_COPY=true`. There is one original attempt per
condition and no automatic retry. Both suite cleanup records are `verified`.

| WebDriver command | Baseline | Preread |
| --- | ---: | ---: |
| `POST /session` | 41.254 s, HTTP 200 | 2.280 s, HTTP 200 |
| First position, `POST /session/.../window/rect`, `{x:0,y:0}` | 300.051 s, HTTP 500 | 0.016 s, HTTP 200 |
| Subsequent size, `{width:1440,height:900}` | Not reached | 0.022 s, HTTP 200 |
| Catalog navigation, `POST /session/.../url` | Not reached | 4.844 s, HTTP 200 |

The per-condition `acceptance/journey/webdriver.log` files record session
responses at line 7 and first-position responses at line 12. Preread's size and
navigation responses are at lines 14 and 16. Baseline's
`acceptance/journey/diagnostics/failure-4.json` records renderer timeout
`300.000`, diagnostic URL `about:blank`, and a screenshot diagnostic timeout.
Its Flutter log shows the web server already serving the compiled target and
then a `WebDriverService.startTest` window-position failure. No Dart compile
error or application assertion failure is recorded; the failure occurs before
the Catalog journey or any framework composing stage is reached.

Preread's `acceptance/journey/run.log`, line 10, records the complete journey
pass. Its `trusted-journey.json` lists two trusted browser clipboard clicks:
`journey-code-copy` and `journey-stream-copy`. All remaining original journey
inputs are Flutter-injected. This pass does not establish complete native-input
or IME acceptance, or close a separate framework composing limitation.

## Actual identity and post-run hashes

Both actual sessions report Microsoft Edge **152.0.4191.53** and EdgeDriver
**152.0.4191.53 (4ee8983fdce2559a0ae8f8376934c5ed353035cd)**. These values appear
in the independent `browser-identity.json` files and report session capabilities.
The `151.0.4129.101` value elsewhere in the CI log belongs to diagnostic unit-test
data and is not the actual session version. The command uses Flutter
`stable-3.47.0-x64`.

Both journey jobs use Ubuntu 24.04.4, image `20260831.293.1`, Linux/X64, and
report `Linux-6.17.0-1022-azure-x86_64-with-glibc2.39`. Their runner identities
and regions differ:

| Condition | Worker ID | Azure region | Top-level `run.log` anchors |
| --- | --- | --- | --- |
| baseline | `f8131e8d-0244-4334-9531-36542737cc79` | `centralus` | lines 402-403 |
| preread | `391097c7-3262-4e1d-b612-984db7d04dcd` | `westcentralus` | lines 7-8 |

Matching image and executable identity therefore does not match the host,
region, filesystem cache residency or instantaneous resource load. The
comparison-only job runs in `westus` and does not execute another journey.

Both reports record these executable identities after the journey attempt and
owned cleanup:

| Executable | Bytes | SHA-256 |
| --- | ---: | --- |
| `/opt/microsoft/msedge/msedge` | 398,466,384 | `6a612f822ea7f30de05d5b36566d3e306cdf8fc7b35577c03efaaddcc2a4923a` |
| `/usr/local/share/edge_driver/msedgedriver` | 23,867,976 | `a26d989f6c81d1b3a03f036af29670f47662aeeeb3631fd27c4c59b192c5ce2b` |

Preread records one intentional read of the actual browser executable before
its journey: **398,466,384 bytes in 6.668552 seconds**. Its path, byte count and
SHA match both post-run browser records. Baseline has no pre-run browser hash;
both driver hashes are post-run only. These timing distinctions preserve the
baseline condition instead of silently adding a read to it.

No source-file or Flutter build-artifact before/after hash manifests are present
in this downloaded set. The equal checkout HEAD and post-run executable hashes
do not establish that every source/build byte remained unchanged throughout
the jobs. Uploaded archive digests identify evidence archives, not runtime
source or Flutter build identity.

## Retained evidence and inference limits

All three artifacts were downloaded once after the run completed. The original
27 extracted files and metadata/log files total **8,224,394 bytes**. Their
paths, byte counts and SHA-256 values are retained in
`/tmp/beautiful-edge-pair-153412b3-33848023805/downloaded-files-manifest.json`
and copied into the companion JSON. The large driver logs and raw resource
JSONL remain in the original directory; this record embeds only bounded
metadata, hashes and outcome details.

| Original artifact | ID | GitHub archive digest |
| --- | ---: | --- |
| [edge-full-baseline](https://api.github.com/repos/pawaovo/shadcn_flutter/actions/artifacts/9927445206) | 9927445206 | `sha256:f40663b8736b1a9454964a6fb19dbc568053604ef094c137310010f7f259063c` |
| [edge-full-preread](https://api.github.com/repos/pawaovo/shadcn_flutter/actions/artifacts/9927346945) | 9927346945 | `sha256:cdafac6c7636a7fa90acda0b68a2eea6c0c51aa4bdbb3e3a9e033554c6709ed8` |
| [edge-full-pair-comparison](https://api.github.com/repos/pawaovo/shadcn_flutter/actions/artifacts/9927450327) | 9927450327 | `sha256:fb2d419f180b98c0447c67d05cfcbbb5c8a87a8fe95dd58c1aca7996716810d3` |

`artifacts-metadata.json`, `run-jobs.json` and the complete `run.log` are retained
at the artifact root. The JSON includes exact API and ZIP URLs plus recorded
expiry timestamps on 2026-09-18. Archive digests above come from GitHub metadata;
the compressed ZIP bytes were not retained for independent digest recomputation.
The absolute `/tmp` paths identify this local capture and are not a durable
remote archive.

The preread result is compatible with investigating executable-read/cache
effects, but this one pair across different regions does not isolate them.
Baseline's original failure remains part of the result. Its 382.602219-second
overall duration and preread's 182.153274 seconds include setup, any preread,
the attempted journey and owned cleanup; different execution paths prevent a
successful-path speedup comparison. The raw resource observations remain
available, but this record makes no resource-cause claim and does not promote
the passing condition into a proven repair or a reproduction-rate estimate.

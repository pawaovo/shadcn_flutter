# Orca capability probe: live debug transport

The standalone GTK capability job in [run 33839985973](https://github.com/pawaovo/shadcn_flutter/actions/runs/33839985973), source `f15f27eb671086444f137475f1cf4e9de3eb6328`, remains **not accepted**. Its wait for a new beta `SPEECH OUTPUT` after KP Enter timed out. A separate local subprocess regression reproduces a defect in the probe's log transport: a completed Orca-style diagnostic line can remain buffered while its process is still running.

The original trace records beta focus, a real basic Where Am I handler at `05:18:45.306945`, and generated beta/check-box/not-checked content. The debug file ends after `PAUSE`, without a final newline, at 101,678 bytes. Speech Dispatcher continues sending samples and records playback END at `05:18:48.179732`; the PCM contains 214,392 bytes with recomputed RMS approximately 4152.47. These facts do not support treating the missing debug line as proof of total silence. They also do not identify the words a human heard or establish completion of the remaining Space action.

The inspected [GNOME Orca 46.1 CLI source](https://raw.githubusercontent.com/GNOME/orca/ORCA_46_1/src/orca/orca_bin.py.in) opens the debug destination as an ordinary text file. Its [debug writer](https://raw.githubusercontent.com/GNOME/orca/ORCA_46_1/src/orca/debug.py) writes lines without flushing. The source hashes and exact downloaded line references are in the [companion record](2026-09-04-orca-capability-log-transport.json). The installed CI source bytes were not separately exported; CI reports version 46.1. No container dependencies were changed.

The base `Probe` now uses the existing `OwnedPtyCapture` transport already used by the Catalog Orca pilot. Orca opens `/dev/stdout` attached to a raw PTY, which selects Python line buffering; the observer copies the original bytes into `orca-debug.log` and flushes them. Owned process cleanup, reader errors and EOF verification remain required. The Catalog pilot override is unchanged.

The regression launches an actual Python subprocess that opens the supplied debug destination, writes a Unicode diagnostic line without flushing, then remains alive. It fails against the old base probe and passes after the transport change, comparing the full original bytes before process exit. All three base-probe tests and all five existing PTY tests pass. The [red log](../diagnostics/orca-capability-log-transport/red.log), [green log](../diagnostics/orca-capability-log-transport/green.log), and [PTY regression log](../diagnostics/orca-capability-log-transport/owned-pty-regression.log) are archived unchanged with hashes.

The original Tab/KP Enter/Space sequence, new beta utterance requirement, real espeak-ng PCM checks, five- and six-second utterance waits, overall bound, and human/application acceptance boundaries remain intact. This local verification used no GUI, reader, synthesis, container build or CI rerun. It establishes a transport repair; a new real capability execution is still required. The original eleven artifact files are retained and hashed, and the unflushed contents of the terminated original process cannot be reconstructed.

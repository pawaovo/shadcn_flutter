# Native performance evidence at 87299572

**P1/P2 passes all eight engineering-budget scenarios; P3 is a complete valid capture with one pass and six budget failures.** The separate initial P3 preparation timed out and measured nothing. Earlier valid failures and failed preparations remain unchanged.

| Run | Captured scenarios | Engine frames / RSS samples | Capture / budget result |
| --- | ---: | ---: | --- |
| `20260904-87299572-p1p2` | 8 | 5,212 / 504 | Driver/script 0; 8/8 budget pass |
| `20260904-87299572-p3-foreground` | 7 | 4,543 / 569 | Driver/script 0; 1/7 budget pass |
| `20260904-87299572-p3` | 0 | 0 / 0 | 120s preparation timeout; driver/script 1 |

## Source and verification

Both valid runs use `8729957220c011329022bafc7a0f7402434ce15e`. Their four before/after manifests have exactly the same 303 files, with independently recomputed digest `9ef9ceb6da0ee9cc7624c2ac6ba3ee41944fe06063ff345bcd6f6c49a85718b8`. A read-only Git blob check matches all 302 tracked inputs byte for byte to this revision; the sole additional input is the stable, generated Flutter Swift `Package.swift`. The unchanged generated `packages/docs` differences are outside the benchmark input scope; the overall worktree is not described as clean.

Independent checks reproduce every raw count, nearest-rank distribution, RSS peak/growth and budget verdict. Every expected scenario has one warmup and three measured rounds with the same ordered operations. All 15 environment records are stable with no transitions: **1728 × 1080 dp, DPR 2, 120 Hz, resumed, frames enabled, platform and framework semantics enabled**, light theme and system motion. The [machine-readable record](./2026-09-04-87299572-native-profile.json) includes complete failed-gate values, sampling intervals and artifact hashes.

The [engineering defaults](./engineering_acceptance.md) remain unchanged and explicitly unapproved as product budgets. A pass is limited to the observed run; component memory, leaks, repeated-run stability and other-platform acceptance are not established.

## P1/P2: observed improvement, all eight workloads pass

| Workload | Build max: 3614250 → 87299572 (ms) | Sampled RSS peak: old → new (MiB) | New frames / RSS |
| --- | ---: | ---: | ---: |
| `search_long_catalog` | 4.880 → 4.279 | 140.141 → 186.812 | 1302 / 115 |
| `code_block_long_source` | 28.981 → 2.089 | 328.609 → 339.125 | 731 / 81 |
| `thinking_long_trace` | 2.727 → 2.762 | 350.312 → 315.031 | 621 / 58 |
| `streaming_long_answer` | 6.335 → 6.418 | 408.484 → 341.625 | 775 / 69 |
| `tool_chips_large_output` | 77.294 → 6.923 | 473.266 → 359.031 | 541 / 52 |
| `chat_long_transcript` | 11.279 → 1.104 | 516.797 → 371.391 | 313 / 35 |
| `filter_table_large_dataset` | 38.296 → 5.306 | 554.625 → 383.828 | 361 / 40 |
| `task_rows_large_workflow` | 5.426 → 5.300 | 596.484 → 387.969 | 568 / 54 |

**Code: 28.981 → 2.089 ms; Tool Chips: 77.294 → 6.923 ms; Chat: 11.279 → 1.104 ms; Filter Table: 38.296 → 5.306 ms.** Recorded datasets and operation sequences match the earlier run. These are combined-candidate before/after observations, not isolated attribution to one optimization.

The highest measured process RSS falls from **596.484375 to 387.96875 MiB**, both in Task Rows. Chat, Filter and Task peak at **371.390625, 383.828125 and 387.96875 MiB**, respectively, below the unchanged 512 MiB gate. Search and Code individually have higher RSS than before; the table retains those increases. The new process-lifetime maximum across recorded snapshots is **448.28125 MiB**, a separate counter that includes work outside measurement.

Lossless timeline transport is present and all decode-failure maps are empty. Compression begins after each sampling window and trailing flush. The smaller retained trace representation and product changes coexist in this run; the RSS change is not proof of a leak fix or a component-exclusive allocation result.

## P3: retain the complete failing observation

| Workload | Build p95 / max (ms) | Raster p95 / max (ms) | Over-interval frames | Sampled RSS peak (MiB) | Budget |
| --- | ---: | ---: | ---: | ---: | --- |
| `prompt_bar` | 0.876 / 10.899 | 0.948 / 3.288 | 1 / 869 | 148.344 | pass |
| `diff_table` | 2.485 / 15.071 | 1.787 / 5.002 | 5 / 495 | 191.594 | budget_fail |
| `records_table` | 1.939 / 111.941 | 8.864 / 23.720 | 72 / 1066 | 254.859 | budget_fail |
| `sidebar_nav` | 1.348 / 20.413 | 5.379 / 18.524 | 15 / 775 | 259.469 | budget_fail |
| `flowchart` | 25.655 / 149.748 | 23.998 / 573.478 | 190 / 349 | 278.203 | budget_fail |
| `insight_cards` | 17.347 / 93.585 | 16.114 / 52.947 | 336 / 731 | 286.797 | budget_fail |
| `selection_actions` | 11.880 / 28.426 | 10.318 / 27.373 | 27 / 258 | 290.625 | budget_fail |

Prompt passes. **Diff fails only the tail-frequency gate: 5/495 = 1.010101…%, greater than 1%.** Its build/raster p95 and maxima pass. This failure occurred before the process starts described below and is not rounded into a pass. Records and every later scenario fail one or more frame gates. All P3 RSS gates pass; the highest sampled peak is **290.625 MiB**, while the separate process-lifetime maximum is **350.8125 MiB**. The Flowchart RSS sampling gap reaches 656.318 ms versus the nominal 100 ms interval; the actual timestamps and samples remain preserved.

## External process timing: association only

A retained **post-capture** `ps` observation reports IINA PID **54418** starting at **2026-09-04 08:18:09 CST**, and VTDecoderXPCService PID **54424** at **08:18:10**. Here CST means Asia/Shanghai, UTC+08:00. Both starts fall inside Records sampling:

- Sampling window: **00:18:08.834856Z → 00:18:21.793095Z**.
- Environment-monitor window: **00:18:08.834857Z → 00:18:21.793099Z**.

The small boundary differences come from separate recorder reads and are kept distinct. The original process observation, timestamp and SHA-256 are embedded/referenced in the JSON companion. Its CPU percentages were observed at **00:26:53.869525Z after capture**, not during the measured window. No playback content was inspected.

Other CI/diagnostic agent commands were paused during both valid measured runs; setup CUA actions preceded READY and assessment followed completion. Other user applications remained open. The process starts and later failures are a temporal association, **not proof of CPU/GPU causality, not a reason to invalidate the capture, and not a reason to delete bad frames**. The operator asked the user to pause video temporarily for a prospective same-source comparison and did not operate IINA. No result from that comparison is claimed here.

## Preparation attempts and preserved history

The first `20260904-87299572-p3` attempt logged `open returned 1`, WAITING, the unchanged 120-second timeout and the workload preparation-precondition failure. Its report has no runtime/preparation snapshot and its frame/RSS objects are empty. `hidden` was observed separately through CUA by the operator; it is not fabricated into the saved JSON.

After that failed attempt, the operator reports that a system `open` command briefly started a profile app without a connected driver (PID 53936). Its absolute path and sole owned process were checked and it was terminated with SIGTERM before the foreground capture. It is not a timed run and has no claimed measurement evidence. For the valid foreground run, system `open -a` returned zero, then Window menu `makeKeyAndOrderFront`, actual resumed state, explicit Start and READY preceded measurement. No LaunchServices root cause is inferred.

The [3614250 baseline](./2026-09-04-3614250-native-baseline.md) remains separate: P1/P2 had five failed scenarios; the earlier valid P3 run retained **4,547 frames / 560 RSS samples with six failures and only Sidebar passing**. Its locked isolated attempt measured zero scenarios. These records are not relabeled or replaced, and no per-scenario best-of-runs result is assembled.

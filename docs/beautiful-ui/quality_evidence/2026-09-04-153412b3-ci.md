# Completed CI evidence for `153412b3`

The exact source commit is `153412b3014192b2d6c4c15e377161a5a862a968` in `pawaovo/shadcn_flutter`. Both push workflows completed naturally on attempt 1. Main CI finished with **10/12 jobs passed**; input and AT capability CI finished with **4/9 jobs passed**. The failures below remain part of this source's evidence, regardless of later changes or separate diagnostic passes.

| Workflow | Run | Final result | Jobs |
|---|---|---|---|
| Beautiful AI UI | [33847999343](https://github.com/pawaovo/shadcn_flutter/actions/runs/33847999343) | failure | 10 success, 2 failure |
| Beautiful AI UI input and AT capability evidence | [33847999382](https://github.com/pawaovo/shadcn_flutter/actions/runs/33847999382) | failure | 4 success, 5 failure |

The [machine record](2026-09-04-153412b3-ci.json) contains every job ID, conclusion, timestamp and URL, all 17 uploaded artifact IDs and GitHub archive digests, and selected original-file sizes and SHA-256 values. Main artifacts were downloaded once to `/tmp/beautiful-ci-153412b3/main`; input artifacts were downloaded once to `/tmp/beautiful-ci-153412b3/input`. Both directories retain full run logs, run/job metadata, download status and local file hash inventories. Read-only network requests were retried after EOF or TLS failures; no CI run was dispatched or retried during collection.

## Main CI results

Formatting checked 164 files with zero changes, and strict analysis passed for all three packages. The library's disjoint groups passed **537 behavior + 109 semantics + 12 strict Linux golden tests = 658 tests**. Catalog passed **109 tests**; upstream shadcn core passed **571 tests**. The behavior command explicitly excluded the semantics and golden directories, so those counts do not overlap.

Web, Linux, Windows, Android and Apple build jobs completed successfully. The original Chrome and Firefox journeys and the Linux, macOS, Windows and iOS native journeys passed. Android's separate journey and the main Edge journey failed as follows.

| Failed job | Observed execution boundary | Original evidence |
|---|---|---|
| [Run Android journey](https://github.com/pawaovo/shadcn_flutter/actions/runs/33847999343/job/100944201351) | APK build and installation passed. The Chat host assertion expected one sent `Check cone inventory` user message and found zero. | `/tmp/beautiful-ci-153412b3/main/job-100944201351-android.log` |
| [Run Edge journey](https://github.com/pawaovo/shadcn_flutter/actions/runs/33847999343/job/100944201257) | The fourth WebDriver request, `POST /session/<id>/window/rect` with `{x:0,y:0}`, returned HTTP 500 after 300.057 seconds. The diagnostic URL remained `about:blank`; Catalog navigation and application assertions were not reached. | `/tmp/beautiful-ci-153412b3/main/edge-journey/diagnostics/failure-4.json` |

The Android assertion is at `integration_test/support/chat_send_diagnostics.dart:140`, called from `catalog_journey_test.dart:236`. Before the single framework touch tap, the composing range was `[-1,-1]` and Send semantics were enabled. At pointer down, pointer up and after the tap, composing was `[11,20]`, Send semantics were disabled, and the host still contained only the two initial messages. This is an actual application-journey assertion failure. These observations do not by themselves establish its complete cause; they are separate from the web framework composing-preservation failures below.

Main Edge's actual session used **Edge 151.0.4129.101** and driver **151.0.4129.101**, revision `cc1d9f4080fd9140611a9600b8d1615db310105d`. The separately retained [paired Edge diagnostic](../diagnostics/edge-startup/2026-09-04-153412b3-pair.md) used **152.0.4191.53** on both of its runners. The pair's provenance match therefore applies only within that pair. Its baseline failure and preread pass neither replace this main failure nor establish a causal fix; the paired runners also used different regions.

## iOS completion

[Build Apple catalogs](https://github.com/pawaovo/shadcn_flutter/actions/runs/33847999343/job/100944201264) passed. Its original `ios-journey.json` reports `passed: true`:

| Stage | Result | Elapsed seconds |
|---|---|---:|
| Install | exit 0 | 34.177 |
| Launch | exit 0 | 8.174 |
| VM service discovery | passed; one scoped history query | 18.078, including launch |
| Native Flutter driver | exit 0; `All tests passed.` | 70.256 |
| Terminate | exit 0 | 8.707 |

The history query itself completed in 9.678 seconds within its recorded 15-second limit. It reports `timed_out: false`, `stdout_eof: true`, `host_group_clean: true`, `final_drain_complete: true`, `host_returncode: 0`, and 227 bytes read. The application PID was 28589. These are this run's observed results, not a promise that future simulator startup will succeed. The machine record hashes the original journey JSON, history capture report and native driver log.

## Input and AT capability results

All four real browser input suites passed **26 W3C stages each**, with real browser clipboard operations. Safari's original full Catalog journey also passed. The combined browser jobs still failed because their separate Flutter-injected framework suites failed:

| Platform | Real browser input | Framework input | Combined job |
|---|---|---|---|
| Chrome 152.0.7977.64 | 26 stages passed | `prompt_composing_and_submission` failed | failure |
| Firefox 154.0.1 | 26 stages passed | `prompt_composing_and_submission` failed | failure |
| Edge 152.0.4191.53 | 26 stages passed | `prompt_composing_and_submission` failed | failure |
| Safari 26.6.2 | 26 stages passed; original full journey passed | `synthetic_resize_preserves_prompt_draft` failed | failure |
| Linux | Native clipboard bridge included in framework suite | 6/6 scenarios passed | success |
| macOS | Native clipboard bridge included in framework suite | 6/6 scenarios passed | success |
| Windows | Native clipboard bridge included in framework suite | 6/6 scenarios passed | success |

Chrome, Firefox and Edge expected the injected composing range `[0,2]` for `中文` and observed `[-1,-1]`. Safari passed that earlier scenario but lost the same composing range in the synthetic resize scenario with `中文 inventory draft`. The text and selection in these reported assertions matched; composing did not. Unicode W3C insertion is not real OS IME acceptance, and Flutter-injected editing values and key events are not native input-method events. The native clipboard bridge results also retain their documented Flutter-injected event boundary.

The Orca native fixture capability job passed: accessibility, navigation, utterance output and synthesized audio were observed. It retained **53,755 PCM frames at 16 kHz**, RMS **3810.714370918401**. Its `application_acceptance` remains `not_accepted`; a GTK fixture does not accept the Flutter Catalog or replace human review.

The Narrator capability job failed with the recorded error **`Owned Narrator window does not support safe minimization`**. Its read-only audio inventory independently recorded **zero render endpoints**; all three default endpoint roles returned `0x80070490`. Native accessibility was observed, while AT navigation, utterance output and synthesized audio remained `not_accepted`. Describing this run solely as a no-audio failure would omit its actual terminal error. The full reports remain in the original input artifacts and are hashed in the machine record.

## Publication validation and remaining scope

The publish dry-run passed with a 3 MB archive and zero warnings. The isolated consumer gate passed against source `153412b3`, resolving public `shadcn_flutter 0.0.54` with archive SHA-256 `403a9e790447dc4b6bae73a810d7ffa52baece4d7b29b32de56d0dd769be080e`. It verified 209 unmodified hosted runtime files, 12 theme observations and all 13 required license labels. The `beautiful_ai_ui` package was a temporary publication-surface path dependency in that consumer; no actual package publication occurred.

This record preserves completed CI results for one source revision. It does not assert complete platform support, real OS IME coverage, human assistive-technology acceptance, visual acceptance or performance acceptance. Historical captures and failures were not rewritten, and later fixes require their own source-specific evidence.
